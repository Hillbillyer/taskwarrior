# Taskwarrior Configuration Files

Run this command to pull the scripts and add the aliases to your .bashrc file.
```
bash <(curl -fsSL https://raw.githubusercontent.com/hillbillyer/taskwarrior/main/setup.sh)
```